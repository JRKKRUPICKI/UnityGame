using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpikesMovement : MonoBehaviour
{
    // private float moveSpeed = 0.0f;
    // private int direction = 1;
    // private float minHeight = -0.15f;
    // private float maxHeight = -0.04f;
    // private Transform finalPosition;

    void Start()
    {
    }

    void Update()
    {
        // finalPosition = GetComponent<Transform>();
        // if(direction == 1){
        //     finalPosition.position = new Vector3(finalPosition.position.x, maxHeight, finalPosition.position.z);
        // }
        // else{
        //     finalPosition.position = new Vector3(finalPosition.position.x, minHeight, finalPosition.position.z);
        // }
        // GetComponent<Transform>().position = Vector3.MoveTowards(GetComponent<Transform>().position, finalPosition.position, moveSpeed * Time.deltaTime);
        // if(GetComponent<Transform>().position == finalPosition.position){
        //     if(direction == 1){
        //         direction = -1;
        //     }
        //     else{
        //         direction = 1;
        //     }
        // }
    }
}
